// import { useAppSelector } from '../../hooks/useHooks';
// export default function BaseRouter() {
//   const initialRoute = useAppSelector((state) => (state.user.status === 'loggedIn' ? '/Focus/app' : '/Focus/'));

//   return null;
// }
